---
title: Què és un Internal Developer Platform?
duracio: Curta
tags: [Docker, Kubernetes, Git]
who: Estudiants informàtics i professionals
background: Coneixements bàsics de programació i/o sistemes informàtics
sala: Auditori
---

export const meta = () => {
  return [
    { title: frontmatter.title }
  ];
};

<div class="flex gap-3.5 pt-4 flex-wrap">
    <div class="flex h-auto min-w-[88px] items-center justify-center rounded-lg bg-[#65E0B8] p-1 text-xs text-neutral-900 ">
        <p>{frontmatter.sala}</p>
    </div>
    {
        frontmatter.tags.map((tag, index) => (
            <div key={index} class="flex h-auto min-w-[88px] items-center justify-center rounded-lg bg-[#FFE999] p-1 text-xs text-neutral-900">
                <p>{tag}</p>
            </div>
        ))
    }
</div>

# {frontmatter.title}

Consisteix en explicar el concepte de 'Platform Engineering' i d'una de les peces clau: un Internal Developer Platform. Es tractaria de fer una petita demo tot utilitzant Kubernetes, Git, ArgoCD i altres eines tot explicant tots els components i conceptes que es veuen involucrats i com seria tot plegat en un entorn productiu.